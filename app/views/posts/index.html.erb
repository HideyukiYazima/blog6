<h1>Blog Posts</h1>
<% @posts.each do |post| %>
  <div>
    <h2><%= post.title %></h2>
    <%= image_tag post.image.url %>
    <p><%= post.content %></p>

    <%= paginate @posts %>
  </div>
<% end %>
